#include<stdio.h>
#include<stdlib.h>
void main()
{  
  int search, i,j,size,c,choice,temp;
  int first,middle,last;
  a[] = { 4 , 7 , 3 , 2 , 1 , 7 , 9 , 0};
    while (1)
    printf("Enter your choice\n 1 . linear search\n 2.Binary search\n 3.exit\n);
    scanf("%d",&choice);
    switch (choice)
    case 1:
    printf("enter the element to be searched");
    scanf("%d",&search);
    for(i=0;i<8;i++)
    if 
     (a[i]==search)
     {
        printf("element found at a index %d,i");
        break;

     }
     if (c=1)
     {
        printf("element not found %d,i");
        break;
     }


}